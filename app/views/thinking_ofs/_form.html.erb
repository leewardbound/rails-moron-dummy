<%= form_for(@thinking_of) do |f| %>
  <% if @thinking_of.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@thinking_of.errors.count, "error") %> prohibited this thinking_of from being saved:</h2>

      <ul>
      <% @thinking_of.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :number %><br />
    <%= f.text_field :number %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
